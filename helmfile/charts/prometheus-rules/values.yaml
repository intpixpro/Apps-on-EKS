# Default values for prometheus-rules.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

nameOverride: ""
fullnameOverride: ""

prometheusRulesFiles: 'rules/*.yml'
